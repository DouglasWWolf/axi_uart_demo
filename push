export project=axi_uart_a7
export folder=nexys_a7_100
git add ${folder}/${project}.gen
git add ${folder}/${project}.hw
git add ${folder}/${project}.ip_user_files
git add ${folder}/${project}.sim
git add ${folder}/${project}.srcs
git add ${folder}/${project}.xpr
git add ${folder}/src
git add ${folder}/xdc

export project=axi_uart_zcu104
export folder=xilinx_zcu104
git add ${folder}/${project}.gen
git add ${folder}/${project}.hw
git add ${folder}/${project}.ip_user_files
git add ${folder}/${project}.sim
git add ${folder}/${project}.srcs
git add ${folder}/${project}.xpr
git add ${folder}/src
git add ${folder}/xdc

git add python cplusplus README.md push

git commit -m "No commit message"
git push origin main
